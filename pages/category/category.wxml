<!--pages/category/category.wxml-->
<scroll-view scroll-back-to-top="{{false}}">
	<text>分类页面</text>

	<!-- 展示数据 -->
	<view>
		<view wx:for="{{list}}" wx:key="index">
			<text bindtap="handleTextClick">{{ item.title }}</text>
			<image src="{{item.image}}"></image>
		</view>
	</view>
</scroll-view>